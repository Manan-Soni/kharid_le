import 'package:flutter/material.dart';

class SellerNavigation extends StatelessWidget{
  const SellerNavigation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Seller UI"),
      ),
    );
  }
}
